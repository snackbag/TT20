import java.util.regex.Pattern

plugins {
    id 'java'

    id 'maven-publish'

    // https://github.com/architectury/architectury-loom
    id 'dev.architectury.loom' version '1.17-SNAPSHOT' apply false
    id 'dev.architectury.loom-no-remap' version '1.17-SNAPSHOT' apply false

    id "me.modmuss50.mod-publish-plugin" version "2.2.0"
}

repositories {
    mavenCentral()
    maven {
        url 'https://www.cursemaven.com'
    }
    maven {
        url = "https://api.modrinth.com/maven"
    }
    maven {
        url 'https://maven.neoforged.net/releases'
    }
    maven {
        url "https://libraries.minecraft.net"
    }
    maven {
        url = "https://artifacts.snackbag.net/artifactory/shitlib"
    }
    maven {
        name = "Terraformers"
        url = "https://maven.terraformersmc.com/"
    }
}

def platformVersion = sc.current.parsed;

if (platformVersion >= "26.1") {
    apply plugin: 'dev.architectury.loom-no-remap'
} else {
    apply plugin: 'dev.architectury.loom'
}

List<String> MIXIN_CONFIG_PATHS = ['tt20.mixins.json']

JavaVersion JAVA_COMPATIBILITY

if (platformVersion >= "26.1") { // 26.1
    JAVA_COMPATIBILITY = JavaVersion.VERSION_25
} else if (platformVersion >= "1.20.5") { // 1.20.5
    JAVA_COMPATIBILITY = JavaVersion.VERSION_21
} else if (platformVersion >= "1.18") { // 1.18
    JAVA_COMPATIBILITY = JavaVersion.VERSION_17
} else if (platformVersion >= "1.17") { // 1.17
    JAVA_COMPATIBILITY = JavaVersion.VERSION_16
} else {
    JAVA_COMPATIBILITY = JavaVersion.VERSION_1_8
}

int javaVersion = JAVA_COMPATIBILITY.ordinal() + 1;

def parchmentVersions = [
        "1.18.2":  "1.18.2:2022.11.06",
        "1.19.2":  "1.19.2:2022.11.27",
        "1.20.1":  "1.20.1:2023.09.03",
        "1.20.3":  "1.20.3:2023.12.31",
        "1.20.5":  "1.20.6:2024.06.16",
        "1.21.1":  "1.21.1:2024.11.17",
        "1.21.2":  "1.21.3:2024.12.07",
        "1.21.9":  "1.21.9:2025.10.05",
        "1.21.11": "1.21.11:2025.12.20"
]

def supportedVersions = [
        "1.18.2":  [ "1.18.2" ],
        "1.19.2":  [ "1.19.2" ],
        "1.20.1":  [ "1.20", "1.20.1", "1.20.2", "1.20.3", "1.20.4" ],
        "1.20.3":  [ "1.20.3", "1.20.4" ],
        "1.20.5":  [ "1.20.5", "1.20.6" ],
        "1.21.1":  [ "1.21", "1.21.1" ],
        "1.21.2":  [ "1.21.2", "1.21.3", "1.21.4", "1.21.5", "1.21.6", "1.21.7", "1.21.8" ],
        "1.21.9":  [ "1.21.9", "1.21.10" ],
        "1.21.11": [ "1.21.11" ],
        "26.1.1":  [ "26.1", "26.1.1", "26.1.2", "26.2" ],
        "26.3":    [ "26.3" ]
]

def getChangelog = {
    def changelog = rootProject.file("CHANGELOG.md").text

    def pattern = ~/(?ms)^${Pattern.quote(project.mod_version)}\R=+\R(.*?)(?=^\S[^\r\n]*\R=+|\z)/
    def matcher = changelog =~ pattern

    if (!matcher.find()) {
        throw new GradleException("Couldn't find changelog for version ${project.mod_version}")
    }

    matcher.group(1).trim()
}

group = project.maven_group
project.version = "${project.mod_version}+mc${project.minecraft_version}-${project.platform}"

base {
    archivesName.set(project.mod_id)
}

stonecutter {
    constants.match(project.platform, 'fabric', 'quilt', 'forge', 'neoforge')
    swaps["mod_version"] = "\"${project.mod_version}\";".toString()
    swaps["mod_id"] = "\"${project.mod_id}\";".toString()
}

configurations {
    modRuntimeOnly.exclude group: 'net.fabricmc', module: 'fabric-loader'
}

dependencies {
    minecraft "com.mojang:minecraft:${project.minecraft_version}"

    if (platformVersion < "26.1") {
        mappings loom.layered() {
            officialMojangMappings()
            parchment("org.parchmentmc.data:parchment-${parchmentVersions[project.minecraft_version]}@zip")
        }
    }

    if (project.platform == 'fabric') {
        if (platformVersion < "26.1") {
            modImplementation "net.fabricmc:fabric-loader:${project.fabric_loader_version}"
            modImplementation("com.terraformersmc:modmenu:${project.modmenu_version}")
        } else {
            implementation "net.fabricmc:fabric-loader:${project.fabric_loader_version}"
            implementation("com.terraformersmc:modmenu:${project.modmenu_version}") {
                exclude group: "net.fabricmc.fabric-api"
            }
        }
        if (platformVersion >= "1.20.5") { // fabric api for modmenu on 1.20.5+
            if (platformVersion >= "26.1") {
                runtimeOnly "net.fabricmc.fabric-api:fabric-api:${project.fabric_api_version}"
            } else {
                modRuntimeOnly "net.fabricmc.fabric-api:fabric-api:${project.fabric_api_version}"
            }

        }
    }
    if (project.platform == 'forge') {
        forge "net.minecraftforge:forge:${project.minecraft_version}-${project.forge_version}"
        compileOnly "org.spongepowered:mixin:0.8.5"

        compileOnly(annotationProcessor("io.github.llamalad7:mixinextras-common:0.5.3"))
        implementation(include("io.github.llamalad7:mixinextras-forge:0.5.3"))
    }
    if (project.platform == 'neoforge') {
        neoForge "net.neoforged:neoforge:${project.neoforge_version}"

        compileOnly(annotationProcessor("io.github.llamalad7:mixinextras-common:0.5.3"))
        implementation(include("io.github.llamalad7:mixinextras-neoforge:0.5.3"))
    }

    implementation "net.snackbag.shit:shit:${project.shit_version}"
}

loom {
    runConfigs.configureEach {
        // to make sure it generates all "Minecraft Client (:subproject_name)" applications
        ideConfigGenerated = true
        vmArgs ['']
    }

    if (project.platform == 'forge') {
        forge {
            mixinConfigs = MIXIN_CONFIG_PATHS
        }
    }
}

processResources {
    inputs.property "id", project.mod_id
    inputs.property "name", project.mod_name
    inputs.property "version", project.mod_version
    inputs.property "minecraft_dependency", project.minecraft_dependency

    def good_files = [] as Set
    def all_files = [] as Set
    [
            'fabric':   MIXIN_CONFIG_PATHS + ['fabric.mod.json'],
            'forge':    MIXIN_CONFIG_PATHS + ['META-INF', 'META-INF/mods.toml', 'pack.mcmeta'],
            'neoforge': MIXIN_CONFIG_PATHS + ['META-INF', 'META-INF/neoforge.mods.toml', 'pack.mcmeta'],
            'paper':    ['plugin.yml'],
    ].forEach { brand, paths ->
        all_files.addAll(paths)
        if (brand == project.platform) {
            good_files.addAll(paths)
        }
    }
    all_files.forEach { it2 ->
        def path = it2 as String
        if (good_files.contains(path)) {
            filesMatching(path) {
                def valueMap = [
                        "id": project.mod_id,
                        "name": project.mod_name,
                        "version": project.mod_version,
                        "minecraft_dependency": project.minecraft_dependency,

                        "description": project.mod_description,
                        "authors": project.mod_authors,
                        "homepage": project.mod_homepage,
                        "sources": project.mod_sources,
                        "issue": project.mod_issue,
                        "license": project.mod_license,
                        "java": JAVA_COMPATIBILITY,
                        "java_compatibility_level": "JAVA_${javaVersion}",
                ]
                expand valueMap
            }
        } else {
            exclude path
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    it.options.release = javaVersion
    options.encoding = "UTF-8"
    options.compilerArgs << "-Xlint:deprecation" << "-Xlint:unchecked"
    if (JAVA_COMPATIBILITY <= JavaVersion.VERSION_1_8) {
        // suppressed "source/target value 8 is obsolete and will be removed in a future release"
        options.compilerArgs << '-Xlint:-options'
    }
}

java {
    sourceCompatibility = JAVA_COMPATIBILITY
    targetCompatibility = JAVA_COMPATIBILITY

    toolchain.languageVersion = JavaLanguageVersion.of(javaVersion)

    withSourcesJar()
}

stonecutter {
    java {
        withSourcesJar()
        sourceCompatibility = JAVA_COMPATIBILITY
        targetCompatibility = JAVA_COMPATIBILITY
    }
}

jar {
    from(rootProject.file('LICENSE')) {
        rename { "${it}_${project.mod_id}" }
    }
}

publishMods {
    displayName = project.mod_version
    version = project.mod_version
    file = platformVersion >= "26.1"
            ? tasks.named("jar").flatMap { it.archiveFile }
            : tasks.named("remapJar").flatMap { it.archiveFile }

    changelog = getChangelog()
    type = STABLE
    modLoaders.add(project.platform)
    if (project.platform == "fabric") modLoaders.add("quilt")

    curseforge {
        projectId = "1368326"
        accessToken = System.getenv("CURSEFORGE_TOKEN")
        minecraftVersions = supportedVersions[project.minecraft_version]
        server = true
        changelogType = "markdown"
    }

    modrinth {
        projectId = "YS3ZignI"
        accessToken = System.getenv("MODRINTH_TOKEN")
        minecraftVersions = supportedVersions[project.minecraft_version]
        environment = SERVER_ONLY
    }
}

publishing {
    publications {
        create("maven${project.name.capitalize()}", MavenPublication) {
            artifactId = project.mod_id

            from components.java
        }
    }

    repositories {
        def mavenToken = System.getenv("MAVEN_TOKEN")

        if (mavenToken != null && !mavenToken.isEmpty()) {
            maven {
                url = uri("https://maven.lemonnik.dev/releases/")

                credentials {
                    username = "lemonnik"
                    password = mavenToken
                }
            }
        }
    }
}